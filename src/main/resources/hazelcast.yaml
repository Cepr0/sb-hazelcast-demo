hazelcast:
  cluster-name: demo
  management-center:
    enabled: true
    url: http://hazelcast-mgmt:8080/hazelcast-mancenter
  network:
    join:
      multicast:
        enabled: true
#  cp-subsystem:
#    cp-member-count: 3

  map:
    default:
      time-to-live-seconds: 60

